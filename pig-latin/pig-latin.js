module.exports = {
	translate : function(stringToTranslate) {
		vowels = ['a','e','i','o','u'];
		//check if the first letter of the string is a vowel
		if (vowels.indexOf(stringToTranslate.charAt(0) != -1)
			return stringToTranslate + 'ay'
	} 
}